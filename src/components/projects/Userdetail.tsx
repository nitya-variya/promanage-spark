import { useUser } from "@clerk/tanstack-react-start";
import React from "react";

const Userdetail = () => {
  const user = useUser();
  // console.log(user);
  return <div></div>;
};

export default Userdetail;
